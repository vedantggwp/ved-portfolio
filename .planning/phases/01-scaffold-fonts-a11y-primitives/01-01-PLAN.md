---
phase: 01-scaffold-fonts-a11y-primitives
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/app/globals.css
  - src/components/SkipLinks.tsx
  - src/components/SectionShell.tsx
  - src/components/VisuallyHidden.tsx
  - src/hooks/useReducedMotion.ts
  - src/lib/sections.ts
autonomous: true
requirements: [FOUND-01, FOUND-05, FOUND-06]

must_haves:
  truths:
    - "Page renders dark warm background (#0A0A0A) with no white flash"
    - "DM Serif Display renders on h1/h2, Inter renders on body text"
    - "Eight section shells exist in DOM order: surface, transition-1, pocket-1, pocket-2, pocket-3, transition-2, projects, contact"
    - "prefers-reduced-motion is detected before any animation code runs"
    - "No hydration mismatch warnings in dev console"
    - "useReducedMotion hook initializes to false on server, syncs on client"
  artifacts:
    - path: "src/app/layout.tsx"
      provides: "Root layout with font loading, skip links, body styling"
      contains: "DM_Serif_Display"
    - path: "src/app/page.tsx"
      provides: "All section shells rendered in depth order"
      contains: "SectionShell"
    - path: "src/app/globals.css"
      provides: "Design tokens: palette, type scale, reset, reduced-motion media query"
      contains: "--color-bg"
    - path: "src/hooks/useReducedMotion.ts"
      provides: "SSR-safe reduced motion detection hook"
      contains: "useReducedMotion"
    - path: "src/lib/sections.ts"
      provides: "Section configuration data"
      contains: "SECTIONS"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/app/globals.css"
      via: "import './globals.css'"
      pattern: "import.*globals\\.css"
    - from: "src/app/layout.tsx"
      to: "next/font/google"
      via: "DM_Serif_Display and Inter font imports"
      pattern: "DM_Serif_Display|Inter"
    - from: "src/app/page.tsx"
      to: "src/lib/sections.ts"
      via: "SECTIONS config drives section rendering"
      pattern: "import.*SECTIONS.*sections"
---

<objective>
Scaffold the Next.js 15 App Router project with DM Serif Display + Inter typography, dark warm palette (#0A0A0A), CSS design token system, section shells for all depth layers, and SSR-safe reduced motion detection.

Purpose: Establish the foundational shell that every subsequent phase builds on -- fonts, colors, section structure, and the reduced-motion baseline.
Output: A running Next.js app with dark background, correct fonts, 8 section shells, and a useReducedMotion hook.
</objective>

<execution_context>
@/Users/ved/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ved/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-scaffold-fonts-a11y-primitives/01-CONTEXT.md
@.planning/phases/01-scaffold-fonts-a11y-primitives/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Next.js project with fonts, palette, and design tokens</name>
  <files>
    src/app/layout.tsx,
    src/app/globals.css,
    src/hooks/useReducedMotion.ts
  </files>
  <action>
1. Initialize the Next.js 15 project in the current directory (ved-portfolio):
   ```bash
   npx create-next-app@latest . --typescript --app --src-dir --no-tailwind --no-eslint --import-alias "@/*"
   ```
   If the directory is non-empty, accept the prompt to proceed (the existing .planning/ and docs/ won't conflict).

2. Replace `src/app/layout.tsx` with root layout that:
   - Imports DM_Serif_Display (weight 400 only, variable '--font-serif') and Inter (variable '--font-sans') from 'next/font/google'
   - Sets `display: 'swap'` and `subsets: ['latin']` for both fonts
   - Applies font CSS variables on `<html>` element: `className={dmSerif.variable} ${inter.variable}`
   - Sets `lang="en"` on `<html>`
   - Imports './globals.css'
   - Renders `<SkipLinks />` before `<main>{children}</main>` in `<body>`
   - Exports metadata: title "Ved Gaikwad", description "I don't build what's expected. I build what's needed."
   - This is a server component (no 'use client')

3. Replace `src/app/globals.css` with the full design token system:
   - CSS reset (box-sizing border-box, margin 0, etc.)
   - `:root` block with:
     - Palette: `--color-bg: #0A0A0A`, `--color-text: #F5F5F5`, `--color-accent: #C4964A`, `--color-text-muted: #A0A0A0`
     - Fluid type scale using clamp() with ~1.25 Major Third ratio: `--text-xs` through `--text-display` (exact values from RESEARCH.md Pattern 2)
     - Font family tokens: `--font-heading: var(--font-serif), 'Georgia', serif` and `--font-body: var(--font-sans), system-ui, sans-serif`
     - Focus ring tokens: `--focus-ring-color: var(--color-accent)`, `--focus-ring-width: 2px`, `--focus-ring-offset: 2px`
     - Section height: `--section-min-height: 100vh`
   - `body` styles: `background: var(--color-bg)`, `color: var(--color-text)`, `font-family: var(--font-body)`, `font-size: var(--text-base)`, `line-height: 1.6`
   - Heading styles: `h1, h2` use `font-family: var(--font-heading)`, each heading level uses its type scale token
   - `@media (prefers-reduced-motion: reduce)` block that kills animations/transitions (from RESEARCH.md Pattern 3)
   - `.visually-hidden` utility class (from RESEARCH.md)
   - `.skip-link` styles (from RESEARCH.md Pattern 5) -- positioned off-screen, visible on focus
   - `:focus-visible` custom focus ring using the tokens
   - `:focus:not(:focus-visible)` removes outline for mouse users

4. Create `src/hooks/useReducedMotion.ts`:
   - 'use client' directive at top
   - Implements SSR-safe hook exactly as in RESEARCH.md Pattern 3
   - Initializes state to `false` (avoids hydration mismatch)
   - Uses `matchMedia('(prefers-reduced-motion: reduce)')` in useEffect
   - Listens for changes and cleans up event listener
   - Returns boolean

Do NOT use Tailwind. Do NOT add 'use client' to layout.tsx. Do NOT request weight 700 for DM Serif Display (only 400 exists).
  </action>
  <verify>
    <automated>cd /Users/ved/ved-portfolio && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>
    - `npm run build` succeeds with zero errors
    - globals.css contains all design tokens (palette, type scale, font families, focus ring)
    - layout.tsx loads both fonts via next/font/google with correct configuration
    - useReducedMotion hook exists with 'use client' directive and SSR-safe initialization
  </done>
</task>

<task type="auto">
  <name>Task 2: Create section shells and wire page</name>
  <files>
    src/lib/sections.ts,
    src/components/SectionShell.tsx,
    src/components/SkipLinks.tsx,
    src/components/VisuallyHidden.tsx,
    src/app/page.tsx
  </files>
  <action>
1. Create `src/lib/sections.ts` with the SECTIONS configuration array (from RESEARCH.md Pattern 4):
   - Export a `Section` type: `{ id: string; label: string; role: string; heading: string | null }`
   - Export `SECTIONS` as a readonly array with these entries:
     - `{ id: 'surface', label: 'Surface', role: 'banner', heading: 'Introduction' }`
     - `{ id: 'transition-1', label: 'Transition', role: 'presentation', heading: null }`
     - `{ id: 'pocket-1', label: 'Strategy', role: 'region', heading: 'Copy to Strategy to Tech' }`
     - `{ id: 'pocket-2', label: 'Accessibility', role: 'region', heading: 'Accessibility Meets Neuromarketing' }`
     - `{ id: 'pocket-3', label: 'Depth', role: 'region', heading: 'Speed as Symptom of Deep Thinking' }`
     - `{ id: 'transition-2', label: 'Transition', role: 'presentation', heading: null }`
     - `{ id: 'projects', label: 'Projects', role: 'region', heading: 'Projects' }`
     - `{ id: 'contact', label: 'Contact', role: 'contentinfo', heading: 'Contact' }`

2. Create `src/components/VisuallyHidden.tsx` (server component, no 'use client'):
   - Accepts `children` and optional `as` prop (defaults to 'span')
   - Renders the element with className "visually-hidden"

3. Create `src/components/SectionShell.tsx` (server component):
   - Accepts a `Section` object as prop
   - Renders a `<section>` with:
     - `id={section.id}`
     - `role={section.role}` (but only if role is NOT the implicit role for that element -- sections with `role="region"` should use `aria-label` instead since `role="region"` is the implicit role when labeled)
     - `aria-label={section.label}` for non-presentation sections
     - `aria-hidden={section.role === 'presentation' ? true : undefined}` -- wait, per RESEARCH.md, do NOT use aria-hidden on transitions. Use `role="presentation"` only.
     - Correction: Add `role="presentation"` only for transition sections. For `banner` and `contentinfo`, use `role` attribute. For `region` sections, use `aria-label` which implicitly creates the landmark.
     - `tabIndex={-1}` on sections that are skip link targets (`projects` and `contact`)
     - `style={{ minHeight: 'var(--section-min-height)' }}`
     - If `section.heading` is not null, render an `<h2>` wrapped in VisuallyHidden (visually hidden but screen-reader-accessible)
     - If `section.heading` is null (transitions), render nothing inside

4. Create `src/components/SkipLinks.tsx` (server component):
   - Renders a `<nav aria-label="Skip links">` with two anchor links:
     - `<a href="#projects" className="skip-link">Skip to projects</a>`
     - `<a href="#contact" className="skip-link">Skip to contact</a>`

5. Replace `src/app/page.tsx` (server component):
   - Imports SECTIONS from '@/lib/sections'
   - Imports SectionShell from '@/components/SectionShell'
   - Maps over SECTIONS array rendering `<SectionShell>` for each
   - No 'use client' directive

Do NOT add interactive roles to sections. Do NOT use aria-hidden on transition sections (per RESEARCH.md pitfall). Surface section uses `role="banner"` not `role="main"` (main element already exists).
  </action>
  <verify>
    <automated>cd /Users/ved/ved-portfolio && npm run build 2>&1 | tail -20 && npm run dev &amp; sleep 3 && curl -s http://localhost:3000 | grep -c 'id="surface"\|id="projects"\|id="contact"\|skip-link\|visually-hidden' && kill %1 2>/dev/null</automated>
  </verify>
  <done>
    - Build succeeds with zero errors
    - page.tsx renders all 8 sections in correct DOM order
    - Skip links target #projects and #contact
    - Sections with headings have visually-hidden h2 elements
    - Transition sections have role="presentation"
    - projects and contact sections have tabIndex={-1} for skip link focus
  </done>
</task>

</tasks>

<verification>
1. `npm run build` completes without errors
2. `npm run dev` renders dark background (#0A0A0A) with correct fonts
3. Page source shows 8 section elements with correct IDs in order
4. No hydration mismatch warnings in browser dev console
5. CSS custom properties are applied (inspect body computed styles)
</verification>

<success_criteria>
- Dark warm page (#0A0A0A) loads with DM Serif Display headings and Inter body text
- All 8 depth-layer sections exist as empty shells with semantic structure
- useReducedMotion hook exists and is SSR-safe
- CSS design token system (palette, type scale, focus tokens) is complete
- Skip links exist in DOM targeting #projects and #contact
- Build passes with zero errors and zero warnings
</success_criteria>

<output>
After completion, create `.planning/phases/01-scaffold-fonts-a11y-primitives/01-01-SUMMARY.md`
</output>
