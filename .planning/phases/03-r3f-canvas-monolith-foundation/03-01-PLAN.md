---
phase: 03-r3f-canvas-monolith-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - next.config.ts
  - package.json
  - src/components/R3FCanvas.tsx
  - src/three/CinematicLighting.tsx
  - src/three/MonolithScene.tsx
  - src/app/layout.tsx
  - tests/r3f.spec.ts
autonomous: true
requirements: [FOUND-04, MONO-06]

must_haves:
  truths:
    - "A fixed R3F canvas renders behind all scrollable DOM content"
    - "Canvas does not block scroll events or pointer interaction with DOM"
    - "Canvas is hidden when prefers-reduced-motion is active"
    - "A warm cinematic light with subtle breathing illuminates the scene"
    - "The page still scrolls correctly with all Phase 2 scroll engine behavior intact"
  artifacts:
    - path: "src/components/R3FCanvas.tsx"
      provides: "Fixed canvas wrapper with pointer-events: none, reduced-motion gate"
      min_lines: 20
    - path: "src/three/MonolithScene.tsx"
      provides: "Scene composition component (lighting + future monolith slot)"
      min_lines: 10
    - path: "src/three/CinematicLighting.tsx"
      provides: "Warm point light with sinusoidal breathing"
      min_lines: 15
    - path: "tests/r3f.spec.ts"
      provides: "E2E tests for canvas presence, z-index, pointer-events, reduced-motion"
      min_lines: 30
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/components/R3FCanvas.tsx"
      via: "dynamic import with ssr: false"
      pattern: "dynamic.*R3FCanvas.*ssr.*false"
    - from: "src/components/R3FCanvas.tsx"
      to: "src/three/MonolithScene.tsx"
      via: "Canvas children"
      pattern: "<Canvas.*>.*<MonolithScene"
    - from: "src/three/MonolithScene.tsx"
      to: "src/three/CinematicLighting.tsx"
      via: "JSX composition"
      pattern: "<CinematicLighting"
---

<objective>
Install R3F dependencies and create a fixed canvas layer behind the DOM with cinematic lighting.

Purpose: Establish the 3D rendering foundation that all subsequent phases build upon. The canvas must coexist with the existing scroll engine without interfering with scroll or pointer events.

Output: Working R3F canvas behind DOM content, warm breathing light, test scaffold for 3D behavior verification.
</objective>

<execution_context>
@/Users/ved/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ved/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-r3f-canvas-monolith-foundation/03-RESEARCH.md

@.planning/phases/02-scroll-engine/02-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs from existing codebase -->

From src/lib/scroll-store.ts:
```typescript
export type ScrollValues = {
  progress: number
  velocity: number
  direction: number
  section: string
  sectionProgress: number
}
export const scrollStore: ScrollValues
```

From src/hooks/useReducedMotion.ts:
```typescript
export function useReducedMotion(): boolean
```

From src/app/layout.tsx:
```typescript
// Current structure: <body> -> <SkipLinks /> -> <ScrollEngine> -> <main>{children}</main>
// R3FCanvas must be inserted BEFORE ScrollEngine, dynamically imported with ssr: false
```

From next.config.ts:
```typescript
// Currently empty config — needs transpilePackages: ['three'] added
const nextConfig: NextConfig = {}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install R3F dependencies and configure Next.js</name>
  <files>package.json, next.config.ts, tests/r3f.spec.ts</files>
  <action>
  1. Install dependencies:
     ```bash
     npm install three @react-three/fiber @react-three/drei
     npm install -D @types/three
     ```

  2. Update `next.config.ts` to add transpilePackages:
     ```typescript
     const nextConfig: NextConfig = {
       transpilePackages: ['three'],
     }
     ```

  3. Create `tests/r3f.spec.ts` test scaffold with these tests (all should initially fail or be marked as expected):
     - "canvas behind DOM": Verify a canvas element exists inside a fixed-position container with z-index 0 and pointer-events none. Check DOM content container has higher z-index.
     - "canvas hidden with reduced motion": With prefers-reduced-motion emulated, verify no canvas element is present in the DOM.
     - "scroll still works with canvas": Scroll the page and verify scrollStore.progress updates (existing scroll behavior unbroken).
     - "WebGL context active": Verify the canvas has a WebGL2 or WebGL rendering context (check canvas.getContext returns non-null via page.evaluate).
     - "lighting present": Verify the R3F scene is rendering (canvas is not blank -- take screenshot and check it's not entirely one color, or check via exposed scene debug).

  The test file should import the existing test patterns from `tests/scroll.spec.ts` (same baseURL, same project config). Use Playwright's `page.emulateMedia` for reduced-motion tests.
  </action>
  <verify>
    <automated>cd /Users/ved/ved-portfolio && npx playwright test tests/r3f.spec.ts --project=chromium 2>&1 | tail -20</automated>
  </verify>
  <done>R3F packages installed, next.config.ts has transpilePackages, test scaffold exists and runs (tests may fail until Task 2 implements components)</done>
</task>

<task type="auto">
  <name>Task 2: Create R3FCanvas, CinematicLighting, MonolithScene and wire into layout</name>
  <files>src/components/R3FCanvas.tsx, src/three/CinematicLighting.tsx, src/three/MonolithScene.tsx, src/app/layout.tsx</files>
  <action>
  1. Create `src/three/CinematicLighting.tsx`:
     - 'use client' directive
     - Single warm point light at position [3, 5, 4] with color "#ffaa44", distance 20, decay 2
     - useFrame reads clock.elapsedTime, modulates intensity: `Math.sin(time * 0.5) * 0.2 + 1.0` (oscillates 0.8-1.2)
     - Dim ambient light: intensity 0.05, color "#1a1510"
     - Store light ref with useRef, mutate intensity directly (never React state)

  2. Create `src/three/MonolithScene.tsx`:
     - 'use client' directive
     - Composes CinematicLighting
     - Placeholder mesh: a simple dark box (1x3x1) as temporary monolith stand-in so the scene is not empty. Use a standard meshStandardMaterial with color "#0a0908" and roughness 0.8. This will be replaced by the GLSL monolith in Plan 02.
     - Camera position already set on Canvas (no need for camera component)

  3. Create `src/components/R3FCanvas.tsx`:
     - 'use client' directive
     - Import useReducedMotion from '@/hooks/useReducedMotion'
     - If prefersReduced, return null (skip entire canvas)
     - Render a wrapper div with: position: fixed, inset: 0, zIndex: 0, pointerEvents: 'none'
     - Inside, render `<Canvas>` from @react-three/fiber with:
       - gl={{ antialias: true, alpha: true }}
       - camera={{ position: [0, 0, 5], fov: 45 }}
       - dpr={[1, 2]}
       - style={{ pointerEvents: 'none' }}
     - Canvas children: `<MonolithScene />`

  4. Update `src/app/layout.tsx`:
     - Add dynamic import: `const R3FCanvas = dynamic(() => import('@/components/R3FCanvas').then(m => m.R3FCanvas), { ssr: false })`
     - Insert `<R3FCanvas />` BEFORE `<ScrollEngine>` inside `<body>`
     - Ensure main content has relative positioning and zIndex 1 (add style={{ position: 'relative', zIndex: 1 }} to main or ScrollEngine wrapper)

  Important: Do NOT use useState for any animation values. All frame-level mutations go through useRef + useFrame.
  </action>
  <verify>
    <automated>cd /Users/ved/ved-portfolio && npx playwright test tests/r3f.spec.ts --project=chromium && npx playwright test tests/scroll.spec.ts --project=chromium</automated>
  </verify>
  <done>R3F canvas renders behind DOM content. Warm breathing light illuminates a placeholder box. Scroll engine works as before. Canvas hidden with reduced motion. All r3f.spec.ts and scroll.spec.ts tests pass.</done>
</task>

</tasks>

<verification>
- `npx playwright test --project=chromium` -- all tests pass (r3f + scroll + a11y)
- Canvas element exists in DOM with correct z-index and pointer-events
- Scroll behavior unaffected (existing 17 tests still green)
- Reduced motion hides canvas entirely
- `npm run build` succeeds with no SSR errors
</verification>

<success_criteria>
1. Fixed R3F canvas at z-index 0 with pointer-events: none behind all DOM content
2. Canvas hidden when prefers-reduced-motion is active
3. Warm cinematic light with breathing modulation visible in scene
4. All existing scroll and a11y tests pass unchanged
5. Next.js build succeeds (no SSR crashes from Three.js imports)
</success_criteria>

<output>
After completion, create `.planning/phases/03-r3f-canvas-monolith-foundation/03-01-SUMMARY.md`
</output>
